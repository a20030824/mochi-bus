--- a/web/map/main.ts
+++ b/web/map/main.ts
@@ -783,6 +783,7 @@ async function chooseCity(city: MapCity) {
   category = '全部'
   routeSearchQuery = ''
   routeScrollTop = 0
+  tripController.reset()
   const { requestId, signal } = beginNavRequest()
   networkButton.hidden = false
   cityNetwork.hide()
@@ -790,7 +791,6 @@ async function chooseCity(city: MapCity) {
   routeDetail.close()
   nearbyLayer.clearLayers()
   previewLayer.clearLayers()
-  tripController.reset()
   setDocumentTitle(`${city.name}公車地圖`)
   setStatus(`${city.name} · 正在整理路線…`)
   renderDrawer({
