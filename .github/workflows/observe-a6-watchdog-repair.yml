name: Observe A6 watchdog repair

on:
  pull_request:
    paths:
      - .github/workflows/observe-a6-watchdog-repair.yml

permissions:
  contents: read
  actions: read

jobs:
  observe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Show recent repair runs
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          curl --fail --location --silent --show-error \
            -H 'Accept: application/vnd.github+json' \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H 'X-GitHub-Api-Version: 2022-11-28' \
            'https://api.github.com/repos/a20030824/mochi-bus/actions/workflows/a6-watchdog-repair.yml/runs?branch=main&event=push&per_page=5' \
          | jq -c '{total_count,runs:[.workflow_runs[]|{id,status,conclusion,head_sha,created_at,updated_at,html_url}]}'
